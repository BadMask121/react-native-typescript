import {app} from '@src/helpers/constants';

const style = {
  inputContainer: {
    height: 50,
    width: '100%',
    marginBottom: 15,
    marginLeft: 10,
  },
  inputStyle: {
    textAlign: 'center',
    fontFamily: app.primaryFontMedium,
    lineHeight: 15,
    fontSize: 15,
    height: 70,
    marginBottom: 10,
  },
};

export default style;
