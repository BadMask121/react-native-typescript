import {app} from '@src/helpers/constants';

const style = {
  ListCardContainer: {
    flex: 1,
    maxHeight: 200,
    backgroundColor: '#fff',
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 1,
    },
    borderRadius: 10,
    shadowOpacity: 0.29,
    shadowRadius: 1.05,
    elevation: 1,
    flexDirection: 'row',
    margin: 10,
  },
};

export default style;
